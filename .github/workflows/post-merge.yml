name: Build and deploy with secure pipeline

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      artifact-name: built-code
      aws-region: eu-west-2
    permissions:
      contents: read

  run-tests:
    uses: ./.github/workflows/test.yml
    permissions:
      contents: read
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  deploy:
    uses: ./.github/workflows/deploy.yml
    needs: 
      - build
      - run-tests
    permissions:
      contents: read
    with:
      artifact-name: built-code
      aws-region: eu-west-2
    secrets:
      GHA_AWS_ROLE_ARN: ${{ secrets.GHA_AWS_ROLE_ARN }}
      GHA_SAM_DEPLOYMENT_BUCKET: ${{ secrets.GHA_SAM_DEPLOYMENT_BUCKET }}
      SIGNING_PROFILE_NAME: ${{ secrets.SIGNING_PROFILE_NAME }}
